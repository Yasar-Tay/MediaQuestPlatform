server:
  port: 8082

spring:
  application:
    name: content-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/mediaquest_content_db}
      auto-index-creation: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

