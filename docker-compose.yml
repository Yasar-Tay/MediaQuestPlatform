services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: mediaquest_db
      POSTGRES_USER: mediaquest_user
      POSTGRES_PASSWORD: mediaquest_password
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
