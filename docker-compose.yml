services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: smartedu_db
      POSTGRES_USER: smartedu_user
      POSTGRES_PASSWORD: smartedu_password
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: